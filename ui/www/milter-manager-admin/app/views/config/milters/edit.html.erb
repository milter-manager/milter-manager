<% form_for(@milter, :url => config_milter_path(@milter)) do |form| %>
  <%= form.error_messages %>

  <table>
    <tr>
      <th><%= form.label("name") %></th>
      <td><%= form.text_field("name") %></td>
    </tr>
    <tr>
      <td rowspawn="2"><%= form.submit("update") %></td>
    </tr>
  </table>
<% end %>
